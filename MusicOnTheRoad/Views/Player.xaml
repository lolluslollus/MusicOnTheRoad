<UserControl
    x:Class="MusicOnTheRoad.Views.Player"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicOnTheRoad.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="1*"/>
			<RowDefinition Height="4*"/>
			<RowDefinition Height="1*"/>
		</Grid.RowDefinitions>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>

			<Button Grid.Column="0" Click="{x:Bind VM.SetSourceFileAsync}">
				<TextBlock Text="Pick a file"/>
			</Button>
			<Button Grid.Column="1" Click="OnPickFolderButton_Click">
				<TextBlock Text="Pick a folder"/>
			</Button>
			<Button Grid.Column="2" Click="{x:Bind VM.AddRootFolderAsync}">
				<TextBlock Text="Add a root folder"/>
			</Button>
			<Button Grid.Column="3" Click="{x:Bind VM.ClearRootFolders}">
				<TextBlock Text="Clear root folders"/>
			</Button>
		</Grid>

		<ListView Grid.Row="1" 
				  ItemsSource="{x:Bind VM.FoldersWithChildren, Mode=OneWay}" 
				  ItemClick="OnListView_ItemClick" 
				  SelectionMode="Single" 
				  SelectionChanged="OnListView_SelectionChanged">
			<!--<ListView.ItemsPanel>
				<ItemsPanelTemplate>
					<VirtualizingStackPanel Orientation="Vertical"/>
				</ItemsPanelTemplate>
			</ListView.ItemsPanel>-->
			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition/>
						</Grid.RowDefinitions>
						<Border BorderThickness="0" 
								IsDoubleTapEnabled="False" IsHoldingEnabled="False" IsRightTapEnabled="False"
								Tapped="OnRootFolderPathBorder_Tapped">
							<StackPanel Orientation="Horizontal">
								<SymbolIcon Symbol="{Binding IsExpanded, Converter={StaticResource BoolToSymbol}}"/>
								<TextBlock Grid.Row="0"
									Text="{Binding FolderPath}"
									TextWrapping="Wrap"
									Margin="10"/>
							</StackPanel>
						</Border>
						<ListView Grid.Row="1"
								ItemsSource="{Binding Children}" 
								ItemClick="OnChildListView_ItemClick" 
								SelectionMode="Single" 
								SelectionChanged="OnChildListView_SelectionChanged">
							<!--<ListView.ItemsPanel>
								<ItemsPanelTemplate>
									<VirtualizingStackPanel Orientation="Vertical"/>
								</ItemsPanelTemplate>
							</ListView.ItemsPanel>-->
							<ListView.ItemTemplate>
								<DataTemplate>
									<Border BorderThickness="0" 
										IsDoubleTapEnabled="False" IsHoldingEnabled="False" IsRightTapEnabled="False"
										Tapped="OnChildFolderBorder_Tapped">
										<TextBlock Text="{Binding}"
											TextWrapping="Wrap"
											Margin="10"/>
									</Border>
								</DataTemplate>
							</ListView.ItemTemplate>
						</ListView>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<MediaPlayerElement x:Name="mediaPlayerElement"
							Grid.Row="2"
							AreTransportControlsEnabled="True"
							AutoPlay="True"							
							Source="{x:Bind VM.Source, Mode=OneWay}"/>
	</Grid>
</UserControl>
